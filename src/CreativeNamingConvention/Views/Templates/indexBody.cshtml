@model IEnumerable<dynamic>
<!--IEnumerable<CreativeNamingConvention.Models.Domain.CreativeOps>-->

<div class="container creative-containter-top">
    <div class="row">
        <div class="col-sm-offset-2 col-sm-8">
            <div class="panel panel-primary">
                <div class="panel-heading panel-height text-center">
                    <h3><b>@ViewBag.Title</b>: Available Otions</h3>
                </div>
                <table class="table table-bordered table-condensed table-responsive">
                    <thead>
                        <tr>
                            <th>@Html.DisplayNameFor(model => model.Name)</th>
                            <th>@Html.DisplayNameFor(model => model.Code)</th>
                            <th>@Html.DisplayNameFor(model => model.Disabled)</th>
                            <th>Edit</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@Html.DisplayFor(modelItem => item.Name)</td>
                                <td>@Html.DisplayFor(modelItem => item.Code)</td>
                                <td>@Html.DisplayFor(modelItem => item.Disabled)</td>
                                <td>@Html.ActionLink("Edit", "Edit", new { id = item.Id })</td>
                            </tr>
                        }
                    </tbody>
                </table>
                <div class="panel-footer">
                    <button class="btn btn-default" style="color:white;"> @Html.ActionLink("Create New", "Create")</button>
                </div>
            </div>
        </div>
    </div>
</div>